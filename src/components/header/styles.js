import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  appBar: {
    background: "none",
  },
  btnIcon: {
    color: "#fff",
    fontSize: "1rem",
  },
  
}));

export default useStyles;
